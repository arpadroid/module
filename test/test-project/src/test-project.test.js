import { describe, it, expect } from '@jest/globals';
describe('Test Project Instance Test', () => {
    it('Just checks if true is true 🤷', () => {
        // eslint-disable-next-line sonarjs/no-trivial-assertions
        expect(true).toBe(true);
    });
});
